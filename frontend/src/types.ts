export type TShopData = {
    phone: string
    instagram: string
    address: string
}

export type TProduct = {
    id: string
    title: string
    price: string
    description: string
    imagePath: string[]
}
